AI Service API Contract
=======================

Scope
-----

Этот сервис отвечает только за AI-функции проекта:

1. извлечение структурированных полей человека из текста
2. OCR / извлечение текста из PDF как fallback для backend
3. сохранение документа в AI-индекс
4. RAG-ответы по документам
5. факт дня по данным архива
6. синтез речи
7. распознавание речи

Сервис не отвечает за:

- регистрацию пользователей
- JWT и авторизацию
- CRUD карточек человека в основной БД
- загрузку файлов с фронта
- хранение истории чатов в основной продуктовой БД

Эти задачи делает основной backend.

Общие правила
-------------

- Все AI-эндпойнты используют `POST`.
- Все идентификаторы приходят из основного backend и являются целыми числами (`int`).
- AI-сервис может хранить у себя технические данные:
  - текст документа
  - ссылку на оригинальный документ
  - чанки
  - эмбеддинги
  - служебные метаданные индексации
- Основной backend по умолчанию передаёт в AI-сервис уже очищенный текст документа.
- Исключение: если backend не смог извлечь текст из PDF на своей стороне, он отправляет PDF в AI-сервис отдельным OCR-запросом и получает готовый текст обратно.
- Формат обмена по умолчанию: `application/json; charset=utf-8`
- Для ASR используется `multipart/form-data`
- Для OCR PDF используется `multipart/form-data`
- Для TTS ответом является бинарное аудио с `Content-Type: audio/mpeg`

Рекомендуемые модели
--------------------

- Extraction / chat: `gpt-5.4`
- OCR / PDF fallback: `gpt-5.4`
- Embeddings: `text-embedding-3-large`
- TTS: `tts-1-hd`
- ASR: `gpt-4o-transcribe`

Режим выбора:

- Сейчас для демо приоритетом является качество, а не стоимость.
- Поэтому используются максимально сильные модели из доступных для данного сценария.
- Если позже понадобится удешевление или ускорение, можно перейти на:
  - `gpt-5.4-mini` для extraction / chat
  - `gpt-5.4-mini` для OCR / PDF fallback
  - `text-embedding-3-small` для embeddings
  - `gpt-4o-mini-tts` для TTS


1. POST /ai/get_info
--------------------

Назначение:
Проанализировать сырой текст документа, определить его тип и вернуть структурированный результат.

Внутри эндпойнта сервис обязан сам классифицировать документ в один из двух типов:

- `single` — документ в основном описывает одного человека;
- `plural` — документ описывает несколько людей, список лиц, сводку, событие или иной многосубъектный контекст.

Если документ относится к типу `single`, сервис извлекает поля карточки человека и сразу подготавливает локализованные версии данных на:

- `ky`
- `ru`
- `en`
- `tr`

Если документ относится к типу `plural`, сервис не пытается строить одну карточку человека и возвращает только список `normalized_names`, найденных в тексте.

Request body:

```json
{
  "text": "Байтемиров Асан Жумабекович, 1899 года рождения..."
}
```

Response body:

Вариант ответа для `single`:

```json
{
  "type": "single",
  "result": {
    "ky": {
      "full_name": "Байтемиров Асан Жумабекович",
      "normalized_name": "байтемиров асан жумабекович",
      "birth_year": 1899,
      "death_year": 1937,
      "birth_date": null,
      "death_date": null,
      "birth_place": "Ак-Башат айылы",
      "death_place": null,
      "region": "Чуй облусу",
      "district": "Кара-Балта",
      "occupation": "Башталгыч мектептин мугалими",
      "charge": "Контрреволюциялык агитация",
      "arrest_date": "1937-08-12",
      "sentence": "Атуу жазасы",
      "sentence_date": "1937-11-03",
      "rehabilitation_date": "1958-04-15",
      "biography": "Ак-Башат айылында төрөлгөн..."
    },
    "ru": {
      "full_name": "Байтемиров Асан Жумабекович",
      "normalized_name": "байтемиров асан жумабекович",
      "birth_year": 1899,
      "death_year": 1937,
      "birth_date": null,
      "death_date": null,
      "birth_place": "село Ак-Башат",
      "death_place": null,
      "region": "Чуйская область",
      "district": "Кара-Балта",
      "occupation": "Учитель начальной школы",
      "charge": "Контрреволюционная агитация",
      "arrest_date": "1937-08-12",
      "sentence": "Расстрел",
      "sentence_date": "1937-11-03",
      "rehabilitation_date": "1958-04-15",
      "biography": "Родился в селе Ак-Башат..."
    },
    "en": {
      "full_name": "Asan Zhumabekovich Baytemirov",
      "normalized_name": "байтемиров асан жумабекович",
      "birth_year": 1899,
      "death_year": 1937,
      "birth_date": null,
      "death_date": null,
      "birth_place": "Ak-Bashat village",
      "death_place": null,
      "region": "Chuy Region",
      "district": "Kara-Balta",
      "occupation": "Primary school teacher",
      "charge": "Counter-revolutionary agitation",
      "arrest_date": "1937-08-12",
      "sentence": "Execution by shooting",
      "sentence_date": "1937-11-03",
      "rehabilitation_date": "1958-04-15",
      "biography": "Born in Ak-Bashat village..."
    },
    "tr": {
      "full_name": "Asan Cumabekoviç Baytemirov",
      "normalized_name": "байтемиров асан жумабекович",
      "birth_year": 1899,
      "death_year": 1937,
      "birth_date": null,
      "death_date": null,
      "birth_place": "Ak-Bashat köyü",
      "death_place": null,
      "region": "Çuy Bölgesi",
      "district": "Kara-Balta",
      "occupation": "İlkokul öğretmeni",
      "charge": "Karşı-devrimci propaganda",
      "arrest_date": "1937-08-12",
      "sentence": "Kurşuna dizilme",
      "sentence_date": "1937-11-03",
      "rehabilitation_date": "1958-04-15",
      "biography": "Ak-Bashat köyünde doğdu..."
    }
  },
  "missing_fields": [
    "death_place"
  ],
  "warnings": []
}
```

Вариант ответа для `plural`:

```json
{
  "type": "plural",
  "normalized_names": [
    "сыдыкова бурул токтогуловна",
    "маматов эсенгул кадырович",
    "исмаилов ракмат назарович",
    "абдуллаев хасан юсупович"
  ],
  "warnings": []
}
```

Примечания:

- Эндпойнт сам определяет `type` на основе содержимого документа. Клиент не передаёт `type` в request.
- `type` обязателен в каждом успешном ответе и может принимать только:
  - `single`
  - `plural`
- Для `single` эндпойнт отвечает и за extraction, и за перевод/локализацию данных карточки.
- Для `single` в ответе всегда должны возвращаться 4 языковых блока:
  - `ky`
  - `ru`
  - `en`
  - `tr`
- Для `single` `normalized_name` должен вычисляться детерминированно внутри сервиса после извлечения, а не как свободный текст модели.
- Для `single` `normalized_name` является техническим полем для поиска и дедупликации, поэтому не обязан переводиться между языками.
- Для `single`, если поле не найдено, возвращается `null`.
- Для `single` числовые и датовые поля должны быть одинаковыми во всех языковых блоках.
- Для `plural` возвращается только список `normalized_names`.
- Для `plural` список должен содержать только детерминированно нормализованные ФИО без дублей.
- Для `plural`, если в документе нет имён людей, возвращается пустой список:

```json
{
  "type": "plural",
  "normalized_names": [],
  "warnings": []
}
```

- Если сервис не уверен в классификации документа или в каком-либо извлечённом значении, он всё равно может вернуть результат, но обязан добавить пояснение в `warnings`.
- Если перевод на один из языков в режиме `single` не удался, сервис может временно вернуть значение на исходном языке, но обязан добавить пояснение в `warnings`.
- Этот эндпойнт ничего не сохраняет в индекс и не пишет в основную БД.
- Для `single` сервис должен проверять, нет ли уже очень похожего человека в локальном архиве (`persons_shadow`).
- Если найден вероятный дубль существующего человека, успешный `200` не возвращается; сервис отвечает `409 Conflict` и сообщает, что новую карточку создавать нельзя.
- Базовый формат ошибки при найденном дубле:

```json
{
  "error": {
    "code": "duplicate_person_detected",
    "message": "a likely duplicate person already exists; do not create a new record",
    "details": {
      "person_id": 2,
      "full_name": "Сыдыкова Бурул Токтогуловна",
      "normalized_name": "сыдыкова бурул токтогуловна",
      "birth_year": 1905,
      "confidence": 0.97,
      "matched_fields": [
        "birth_year",
        "arrest_date",
        "sentence_date",
        "rehabilitation_date",
        "region",
        "district",
        "occupation",
        "charge",
        "sentence"
      ]
    }
  }
}
```


2. POST /ai/extract_pdf_text
----------------------------

Назначение:
Извлечь текст из PDF-файла, если основной backend не смог получить текст на своей стороне.

Этот эндпойнт является fallback-шагом перед `get_info` и `save_doc`.

Важно:

- Это не отдельная "OCR-модель" как отдельный продуктовый класс.
- Для OCR используются vision-capable OpenAI models и PDF input support в OpenAI API.
- Если до AI-сервиса дошёл этот запрос, сервис не пытается сначала локально вытащить text layer своими средствами.
- Сервис сразу использует OCR/PDF extraction flow через OpenAI и возвращает готовый текст.

Request:

- `multipart/form-data`
- поле `file`: PDF-файл

Пример ответа:

```json
{
  "text": "Сыдыкова Бурул Токтогуловна, 1905 года рождения...",
  "warnings": [],
  "extraction_mode": "vision_ocr"
}
```

Примечания:

- Эндпойнт возвращает только текст PDF и служебные предупреждения.
- Эндпойнт не делает extraction карточки человека, не индексирует документ и не пишет ничего в БД.
- После получения текста backend сам решает, что делать дальше:
  - вызвать `POST /ai/get_info`
  - вызвать `POST /ai/save_doc`
  - показать текст пользователю для подтверждения
- Если файл не является PDF, сервис должен вернуть `415 unsupported_media_type`.
- Если PDF повреждён, защищён паролем или текст не удалось извлечь, сервис должен вернуть ошибку.
- Допустимые значения `extraction_mode`:
  - `vision_ocr`


3. POST /ai/save_doc
--------------------

Назначение:
Сохранить текст документа в локальный AI-индекс, нарезать на чанки и посчитать эмбеддинги.

Request body:

```json
{
  "person_id": 12,
  "document_id": 18,
  "filename": "delo_baytemirova.txt",
  "link": "https://archive.example/documents/18",
  "text": "Полный текст документа...",
}
```


Response body:

```json
{
  "status": "ok"
}
```

Примечания:

- Этот эндпойнт нужен только для AI-индекса.
- Основной backend должен сам решать, где хранится оригинальный файл.
- AI-сервис сохраняет у себя:
  - текст документа
  - ссылку на оригинальный документ
  - чанки
  - embeddings
  - техническую метаинформацию индексации
- Поле `link` обязательно: это непустая строка со ссылкой или внешним идентификатором документа, который frontend сможет открыть для перепроверки источника.
- Если `document_id` уже существует, поведение должно быть одним из двух:
  - переиндексация документа
  - ошибка `409 conflict`

Рекомендуемое решение:

- Для MVP лучше поддержать переиндексацию по тому же `document_id`.


4. POST /ai/chat
----------------

Назначение:
Получить ответ на вопрос по уже сохранённым документам через RAG.

Request body:

```json
{
  "question": "За что его арестовали?",
  "history": [
    {
      "question": "...",
      "answer": "..."
    },
    {
      "question": "...",
      "answer": "..."
    }
  ]
}
```

Response body:

```json
{
  "answer": "Байтемиров был арестован по обвинению в контрреволюционной агитации...",
  "sources": [
    {
      "document_id": 18
    }
  ]
}

```


Примечания:

- `history` передаётся вызывающей стороной, AI-сервис не обязан хранить историю сам.
- AI-сервис сам выбирает search scope в зависимости от вопроса:
  - для person-centric вопросов сначала пытается определить человека и сузить корпус
  - для глобальных вопросов может искать по более широкому набору документов
- `sources` обязательны, потому что это напрямую улучшает качество демо и соответствует ТЗ.
- Каждый source должен содержать только `document_id`.
- Если ответа в документах нет, сервис должен явно сказать, что в доступном контексте недостаточно данных.

Минимальное поведение:

- взять embedding вопроса
- найти top-k чанков
- собрать prompt
- вернуть ответ только на основе найденных документов


5. POST /ai/fact_of_day
------------------------

Назначение:
Вернуть один готовый текст "факта дня" по уже загруженным архивным данным.

Этот эндпойнт ничего не принимает и вызывается отдельно, например раз в день по cron/job со стороны backend.

Request body:

- отсутствует

Response body:

```json
{
  "text": "Факт дня: 68 лет назад, 4 апреля 1958 года, в архиве отмечена реабилитация Байтемирова Асана Жумабековича."
}
```

Примечания:

- Эндпойнт всегда использует уже сохранённые данные из локального AI-архива.
- Для MVP факт дня выбирается детерминированно без отдельного пользовательского input.
- Предпочтение отдаётся годовщинам архивных событий:
  - `arrest_date`
  - `sentence_date`
  - `rehabilitation_date`
- Если на текущую дату подходящих годовщин нет, сервис выбирает fallback-факт из наиболее содержательных и редких кейсов архива.
- Эндпойнт возвращает только готовый текст и не пишет ничего в БД.


6. POST /ai/voice
-----------------

Назначение:
Получить текст, перефразировать в приятный текст и перевести данный текст в аудио.

Request body:

```json
{
  "text": "Ислам, 19 января, место рождения - Бишкек",
  "language": "ru"
}
```

Response:

- binary audio
- `Content-Type: audio/mpeg`

Примечания:

- Этот эндпойнт занимается саммаризацией текста и TTS.
- Язык озвучивания передаётся вызывающей стороной в поле `language`.
- Поддерживаемые языки озвучивания только:
  - `ky`
  - `ru`
  - `tr`
  - `en`
- Перед озвучкой текст должен быть приведён к приятной, связной форме средней длины.
- Нельзя искажать факты, даты, имена, места, должности, обвинения и другие важные исторические данные.
- Если входной текст уже достаточно хороший, допускается только мягкая стилистическая правка без изменения смысла.


7. POST /ai/asr
---------------

Назначение:
Распознать речь из аудиофайла и вернуть текст.

Request:

- `multipart/form-data`
- поле `file`: аудиофайл

Пример ответа:

```json
{
  "text": "Ассаламу алейкум, Жомарт! Кандайсың?"
}
```

Примечания:


- Возвращаем только распознанный текст.
- Поддерживаются только языки:
  - `ky`
  - `ru`
  - `tr`
  - `en`
- Если аудио распознано как неподдерживаемый язык, сервис должен вернуть ошибку валидации.


Ошибки
------

Единый формат ошибок:

```json
{
  "error": {
    "code": "bad_request",
    "message": "required field is missing"
  }
}
```

Рекомендуемые коды:

- `400 bad_request`
- `404 not_found`
- `409 conflict`
- `415 unsupported_media_type`
- `422 validation_error`
- `500 internal_error`


Что нужно сделать в реализации
------------------------------

MVP-порядок:

1. `POST /ai/get_info`
2. `POST /ai/save_doc`
3. `POST /ai/chat`
4. `POST /ai/extract_pdf_text`
5. `POST /ai/fact_of_day`
6. `POST /ai/voice`
7. `POST /ai/asr`

Почему так:

- первые три эндпойнта закрывают основную ценность хакатона
- `voice` и `asr` полезны, но не критичны для MVP


Интеграционный комментарий для backend-команды
----------------------------------------------

Основной backend должен:

- хранить продуктовые сущности у себя
- передавать в AI-сервис `person_id`, `document_id`, `link` и текст документа
- если извлечение текста из PDF на backend не удалось, вызывать `POST /ai/extract_pdf_text`
- если нужен ежедневный готовый текст для витрины/баннера, вызывать `POST /ai/fact_of_day`
- сам хранить историю комнат и сообщений, если это продуктовая логика
- использовать ответ `sources` для отображения ссылок на источники в UI

AI-сервис должен:

- извлекать текст из PDF через OCR/PDF extraction flow как fallback
- индексировать документы
- искать релевантные чанки
- генерировать ответ
- собирать факт дня по архивным данным
- делать TTS
- делать ASR
